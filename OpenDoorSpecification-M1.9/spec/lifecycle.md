# Lifecycle — Draft

ODS distinguishes normal completion, user-requested exit, timeout, carrier loss, host shutdown, and adapter error. Historical adapters must map host-specific return codes or messages into these states without hiding a disconnect.
