# FAME DoorPort — current findings

The uploaded FAME material contains the DoorHeader utility documentation and FAME command guides. M1.7 catalogs these sources but does not yet claim a complete 380-command DoorPort ABI.

Next verification steps:

1. extract command names and numeric identifiers from `FAMECommands.guide`;
2. distinguish CLI DoorHeader generation from live DoorPort messaging;
3. map lifecycle, user, terminal, file and message commands;
4. confirm behavior against a running FAME installation.
