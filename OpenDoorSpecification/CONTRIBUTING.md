# Contributing

Contributions are welcome. Historical API additions must include reproducible evidence: archive name, SHA-256 hash, internal file path, and a concise description of what the source establishes. Do not submit copyrighted SDK archives themselves unless redistribution is explicitly permitted.

Before submitting changes, run:

```bash
python -m unittest discover -s tests
python -m unittest discover -s tools/ods-tools/tests
```
