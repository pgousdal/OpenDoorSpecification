# Terminal — Draft

The terminal model separates byte/text output, blocking key input, non-blocking polling, line input, file rendering, and output cancellation. Encoding and ANSI capabilities are negotiated rather than assumed.
